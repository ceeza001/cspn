import from "react";
import { ContainerScroll } from "@/components/ui/container-scroll-animation";

const Boost = () => {
  return (
    <div className="p-[1rem] md:py-0 overflow-hidden">
      <ContainerScroll />
    </div>
  );
}

export default Boost;